<?xml version="1.0"?>
<!-- Soccer Robot -->
<robot name="soccer_robot" xmlns:xacro="http://www.ros.org/wiki/xacro">
  <xacro:property name="PI" value="3.1415926535897931"/>
  <xacro:macro name="wheel" params="parent id mass radius thickness damping xy yaw axis">
    <link name="base${id}">
      <collision name="collision">
        <origin xyz="${xy} 0" rpy="0 0 0"/>
        <geometry>
          <cylinder
            radius="${radius}"
            length="${thickness}"/>
        </geometry>
      </collision>
      <visual>
        <origin xyz="${xy} 0" rpy="0 0 0"/>
        <geometry>
          <cylinder
            radius="${radius}"
            length="${thickness}"/>
        </geometry>
      </visual>
      <inertial>
        <origin xyz="${xy} 0" rpy="0 0 0"/>
        <mass value="${mass}"/>
        <inertia
          ixx="${mass/12 * (3*radius*radius + thickness*thickness)}" ixy="0.0" ixz="0.0"
          iyy="${mass/12 * (3*radius*radius + thickness*thickness)}" iyz="0.0"
          izz="${mass*radius*radius/2}"/>
      </inertial>
    </link>

    <gazebo reference="base${id}">
      <material>Gazebo/Red</material>
    </gazebo>

    <link name="wheel${id}">
      <collision name="collision">
        <origin xyz="${xy} ${radius}" rpy="0 ${PI/2} ${yaw}"/>
        <geometry>
          <cylinder
            radius="${radius}"
            length="${thickness}"/>
        </geometry>
      </collision>
      <visual name="visual">
        <origin xyz="${xy} ${radius}" rpy="0 ${PI/2} ${yaw}"/>
        <geometry>
          <box size="${radius*.707} ${radius*.707} ${thickness}"/>
        </geometry>
      </visual>
      <inertial>
        <origin xyz="${xy} ${radius}" rpy="0 ${PI/2} ${yaw}"/>
        <mass value="${mass}"/>
        <inertia
          ixx="${mass/12 * (3*radius*radius + thickness*thickness)}" ixy="0.0" ixz="0.0"
          iyy="${mass/12 * (3*radius*radius + thickness*thickness)}" iyz="0.0"
          izz="${mass*radius*radius/2}"/>
      </inertial>
    </link>

    <joint type="continuous" name="base_joint${id}">
      <origin xyz="-.2 0 .2" rpy="0 0 0"/>
      <parent link="${parent}"/>
      <child link="base${id}"/>
    </joint>

    <joint type="continuous" name="joint${id}">
      <origin xyz="-.2 0 .2" rpy="0 0 0"/>
      <parent link="base${id}"/>
      <child link="wheel${id}"/>
      <axis xyz="1 0 0"/>
    </joint>

    <transmission name="tran${id}">
      <type>transmission_interface/SimpleTransmission</type>
      <joint name="joint${id}">
        <hardwareInterface>EffortJointInterface</hardwareInterface>
      </joint>
      <actuator name="motor${id}">
        <hardwareInterface>EffortJointInterface</hardwareInterface>
        <mechanicalReduction>1</mechanicalReduction>
      </actuator>
    </transmission>
  </xacro:macro>
</robot>
